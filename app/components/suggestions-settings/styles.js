import { buttons } from '../../constants/style'

export default {
  activeQueueButton: {
    ...buttons.defaultButton,
  },
  queueButton: {
    ...buttons.defaultButton,
    opacity: 0.3,
  },
  queueButtonText: {
    ...buttons.defaultButtonText,
  },
}
